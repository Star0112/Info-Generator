const fs = require('fs');

async function main() {
    // for (let i = 100; i < 1000; i = i + 1) {
    //     await fs.renameSync(`resized-images/0${i}.png`, `resized-images/${i}.png`);
    // }
    console.log("Completed!!!")
}

main();
